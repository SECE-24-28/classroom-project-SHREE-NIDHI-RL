{
  "name": "classroom-project",
  "version": "1.0.0",
  "scripts": {
    "start": "node Backend/entry.js",
    "build": "npm run install-deps && npm run build-frontend",
    "install-deps": "cd Backend && npm install && cd ../Frontend && npm install",
    "build-frontend": "cd Frontend && npm run build",
    "dev": "concurrently \"cd Backend && npm start\" \"cd Frontend && npm run dev\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "bcrypt": "^6.0.0",
    "express": "^5.2.1",
    "mongoose": "^9.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.11"
  }
}
